# Interactive wrapper

Choose declarative `data-bs-*` behavior when the documented component supports it. Choose a public JavaScript API only when Angular needs explicit initialization, events, or cleanup. Never mix the two strategies on one element.

The wrapper owns inputs, outputs, IDs, browser-only lifecycle, and cleanup. Bootstrap Italia owns the documented interaction. Verify the exact component page and installed version before writing constructor names, methods, events, or options.

